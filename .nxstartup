. "${HOME}/.xinit"

STARTKDE=/opt/kde3/bin/startkde
if [ -z "${STARTKDE}" ]; then
    STARTKDE="$(type -p startkde)"
fi
if [ -z "${STARTKDE}" ]; then
    STARTKDE=xterm
fi

if type -p e16 > /dev/null; then
    export KDEWM=e16
fi

exec "${STARTKDE}"
